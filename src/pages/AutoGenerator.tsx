"use client"; import React, { useState, useEffect } from "react"; import { useApp } from "../context/AppContext"; import { showSuccess, showError } from "../utils/toast"; import PageHeader from "../components/shared/PageHeader"; import { DAYS, PERIODS } from "../constants/schedule"; import { Sparkles } from "lucide-react"; import { Button } from "@/components/ui/button"; import { PeriodPart } from "@/types"; // Modular Sub-components import RequirementForm from "../components/auto-generator/RequirementForm"; import RequirementTable from "../components/auto-generator/RequirementTable"; import GenerationStatsCard from "../components/auto-generator/GenerationStatsCard"; import UnplacedLessonsCard from "../components/auto-generator/UnplacedLessonsCard"; import GeneratorRulesCard from "../components/auto-generator/GeneratorRulesCard"; interface Requirement { id: string; employeeId: string; subjectId: string; classId: string; room: string; count: number; // Number of periods per week } const AutoGenerator = () => { const { employees, classes, subjects, rooms, assignments, setAssignments, isRTL, t, periodConfigs } = useApp(); // Load requirements from localStorage if available const [requirements, setRequirements] = useState<Requirement[]>(() => { const saved = localStorage.getItem("auto_generator_requirements"); return saved ? JSON.parse(saved) : []; }); const [isGenerating, setIsTransolving] = useState(false); const [generatedAssignments, setGeneratedAssignments] = useState<any[]>([]); const [unplacedLessons, setUnplacedLessons] = useState<any[]>([]); const [generationStats, setGenerationStats] = useState<{ successRate: number; total: number; placed: number } | null>(null); // Load rules and constraints from localStorage if available const [rules, setRules] = useState(() => { const saved = localStorage.getItem("auto_generator_rules"); if (saved) { try { const parsed = JSON.parse(saved); // Ensure allowSingleHour has a default value if not present in saved rules if (parsed.allowSingleHour === undefined) { parsed.allowSingleHour = true; } return parsed; } catch (e) { console.error("Failed to parse saved rules", e); } } return { allowedDays: [0, 1, 2, 3, 4], // Sunday to Thursday by default allowedPeriods: ["1", "2", "3", "4", "5", "6", "7", "8"], // Default active periods maxTeacherHoursPerDay: 6, maxTeacherConsecutiveHours: 3, maxClassHoursPerDay: 6, avoidTeacherGaps: false, selectedClassIds: ["all"], // Target branches respectExistingLessons: true, // Respect already scheduled lessons selectedPeriodParts: ["Morning", "Afternoon", "Evening"] as PeriodPart[], // Target periods allowSingleHour: true // Allow single-hour lessons for teachers by default }; }); // Form state for adding a new requirement manually const [newReq, setNewReq] = useState({ employeeId: "", subjectId: "", classId: "", room: "", count: 1 }); // Save rules to localStorage whenever they change useEffect(() => { localStorage.setItem("auto_generator_rules", JSON.stringify(rules)); }, [rules]); // Save requirements to localStorage whenever they change useEffect(() => { localStorage.setItem("auto_generator_requirements", JSON.stringify(requirements)); }, [requirements]); // Extract requirements from current schedule const handleExtractFromCurrent = () => { if (assignments.length === 0) { showError(isRTL ? "لا توجد حصص في الجدول الحالي لاستخراجها" : "No lessons in current schedule to extract"); return; } // Filter assignments based on selected classes if not "all" const targetAssignments = rules.selectedClassIds.includes("all") ? assignments : assignments.filter(a => rules.selectedClassIds.includes(a.classId)); if (targetAssignments.length === 0) { showError(isRTL ? "لا توجد حصص للفروع المحددة لاستخراجها" : "No lessons found for the selected branches to extract"); return; } // Group assignments to find weekly counts const groups: Record<string, { req: Omit<Requirement, 'id' | 'count'>; count: number }> = {}; targetAssignments.forEach(asgn => { const key = `${asgn.employeeId}-${asgn.subjectId}-${asgn.classId}-${asgn.room || ""}`; if (!groups[key]) { groups[key] = { req: { employeeId: asgn.employeeId, subjectId: asgn.subjectId, classId: asgn.classId, room: asgn.room || "" }, count: 0 }; } groups[key].count++; }); const extracted: Requirement[] = Object.entries(groups).map(([key, val]) => ({ id: `req-${Math.random().toString(36).substr(2, 9)}`, ...val.req, count: val.count })); setRequirements(extracted); showSuccess(isRTL ? `تم استخراج ${extracted.length} متطلبات تدريس بنجاح` : `Extracted ${extracted.length} teaching requirements`); }; const handleAddRequirement = () => { if (!newReq.employeeId || !newReq.subjectId || !newReq.classId) { showError(isRTL ? "يرجى ملء الحقول الأساسية (الأستاذ، المادة، الفوج)" : "Please fill required fields"); return; } // If target branches are specified, warn if adding a requirement for a non-selected branch if (!rules.selectedClassIds.includes("all") && !rules.selectedClassIds.includes(newReq.classId)) { showError(isRTL ? "الفوج المحدد ليس من ضمن الفروع المستهدفة بالجدولة حالياً" : "The selected class is not in the target branches list"); return; } const id = `req-${Math.random().toString(36).substr(2, 9)}`; setRequirements([...requirements, { id, ...newReq }]); setNewReq({ employeeId: "", subjectId: "", classId: "", room: "", count: 1 }); showSuccess(isRTL ? "تم إضافة متطلب التدريس" : "Requirement added"); }; const handleDeleteRequirement = (id: string) => { setRequirements(requirements.filter(r => r.id !== id)); }; const handleClearRequirements = () => { setRequirements([]); setGeneratedAssignments([]); setUnplacedLessons([]); setGenerationStats(null); }; // Helper to check maximum consecutive hours for a teacher on a day const checkConsecutiveHours = ( teacherId: string, day: number, newPeriod: string, currentAssignments: any[], maxConsecutive: number ): boolean => { const periods = currentAssignments .filter(a => a.employeeId === teacherId && a.day === day) .map(a => parseInt(a.period)); periods.push(parseInt(newPeriod)); periods.sort((a, b) => a - b); let maxSeq = 0; let currentSeq = 0; let lastVal = -99; for (const p of periods) { if (p === lastVal + 1) { currentSeq++; } else { currentSeq = 1; } maxSeq = Math.max(maxSeq, currentSeq); lastVal = p; } return maxSeq <= maxConsecutive; }; // Helper to check if placing a lesson creates a gap for a teacher on a day const checkTeacherGaps = ( teacherId: string, day: number, newPeriod: string, currentAssignments: any[] ): boolean => { const existingPeriods = currentAssignments .filter(a => a.employeeId === teacherId && a.day === day) .map(a => parseInt(a.period)); if (existingPeriods.length === 0) return true; const pNum = parseInt(newPeriod); return existingPeriods.includes(pNum - 1) || existingPeriods.includes(pNum + 1); }; // Smart Scheduling Algorithm with Rules Enforcement const calculateConflicts = (assignments: Assignment[]) => { const teacherMap: Record<string, any[]> = {}; const roomMap: Record<string, any[]> = {}; assignments.forEach(a => { if (a.employeeId) { (teacherMap[a.employeeId] ??= []).push(a); } if (a.room) { (roomMap[a.room] ??= []).push(a); } }); const teacherConflicts = Object.values(teacherMap).filter(arr => arr.length > 1).map(arr => ({ day: arr[0].day, period: arr[0].period, employeeId: arr[0].employeeId, assignments: arr })); const roomConflicts = Object.values(roomMap).filter(arr => arr.length > 1).map(arr => ({ day: arr[0].day, period: arr[0].period, room: arr[0].room, assignments: arr })); return { teacherConflicts, roomConflicts, total: teacherConflicts.length + roomConflicts.length }; }; const handleGenerate = () => { // Filter requirements to only include selected classes if not "all" const activeRequirements = rules.selectedClassIds.includes("all") ? requirements : requirements.filter(r => rules.selectedClassIds.includes(r.classId)); if (activeRequirements.length === 0) { showError(isRTL ? "لا توجد متطلبات تدريس للفروع المحددة" : "No teaching requirements found for the selected branches"); return; } if (rules.allowedDays.length === 0) { showError(isRTL ? "يرجى تفعيل يوم واحد على الأقل في القواعد" : "Please select at least one active day"); return; } if (rules.allowedPeriods.length === 0) { showError(isRTL ? "يرجى تفعيل حصة واحدة على الأقل في القواعد" : "Please select at least one active period"); return; } if (rules.selectedPeriodParts.length === 0) { showError(isRTL ? "يرجى تفعيل فترة واحدة على الأقل (صباحية، بعد الزوال، مسائية)" : "Please select at least one active period part"); return; } setIsTransolving(true); setTimeout(() => { // Flatten requirements into individual lessons to place let lessonsToPlace: any[] = []; activeRequirements.forEach(req => { for (let i = 0; i < req.count; i++) { lessonsToPlace.push({ id: `gen-${Math.random().toString(36).substr(2, 9)}`, employeeId: req.employeeId, subjectId: req.subjectId, classId: req.classId, room: req.room }); } }); // Sort lessons to place: place harder ones first const teacherCounts: Record<string, number> = {}; lessonsToPlace.forEach(l => { teacherCounts[l.employeeId] = (teacherCounts[l.employeeId] || 0) + 1; }); lessonsToPlace.sort((a, b) => (teacherCounts[b.employeeId] || 0) - (teacherCounts[a.employeeId] || 0)); // Get active slots filtered strictly by allowedDays, allowedPeriods, selectedPeriodParts, AND active periodConfigs const activeSlots: { day: number; period: string }[] = []; DAYS.forEach(day => { if (rules.allowedDays.includes(day.id)) { PERIODS.forEach(period => { if (rules.allowedPeriods.includes(period)) { const pNum = parseInt(period); let part: PeriodPart = "Morning"; if (pNum >= 5 && pNum <= 7) part = "Afternoon"; else if (pNum >= 8) part = "Evening"; if (rules.selectedPeriodParts.includes(part)) { const config = periodConfigs.find(c => c.day === day.id && c.period === period); if (config?.isActive !== false) { activeSlots.push({ day: day.id, period }); } } } }); } }); if (activeSlots.length === 0) { showError(isRTL ? "لا توجد حصص زمنية مفعلة تطابق القواعد المحددة" : "No active periods match the specified rules"); setIsTransolving(false); return; } // Load existing assignments if "Respect Existing Lessons" is enabled const baseAssignments = rules.respectExistingLessons ? [...assignments] : []; let bestAssignments: any[] = []; let bestUnplaced: any[] = []; let bestConflictCount = Infinity; const RESTARTS = 100; for (let run = 0; run < RESTARTS; run++) { // Reset current assignments for this run let currentAssignments = [...baseAssignments]; let placedCount = 0; const unplaced: any[] = []; // Shuffle slots to randomize placement order const shuffledSlots = [...activeSlots].sort(() => Math.random() - 0.5); lessonsToPlace.forEach(lesson => { let placed = false; for (const slot of shuffledSlots) { const teacherBusy = currentAssignments.some(a => a.employeeId === lesson.employeeId && a.day === slot.day && a.period === slot.period); if (teacherBusy) continue; const classBusy = currentAssignments.some(a => a.classId === lesson.classId && a.day === slot.day && a.period === slot.period); if (classBusy) continue; if (lesson.room && currentAssignments.some(a => a.room === lesson.room && a.day === slot.day && a.period === slot.period)) continue; const teacherHours = currentAssignments.filter(a => a.employeeId === lesson.employeeId && a.day === slot.day).length; if (teacherHours >= rules.maxTeacherHoursPerDay) continue; const classHours = currentAssignments.filter(a => a.classId === lesson.classId && a.day === slot.day).length; if (classHours >= rules.maxClassHoursPerDay) continue; const consecutiveValid = checkConsecutiveHours(lesson.employeeId, slot.day, slot.period, currentAssignments, rules.maxTeacherConsecutiveHours); if (!consecutiveValid) continue; if (rules.avoidTeacherGaps) { const isGapValid = checkTeacherGaps(lesson.employeeId, slot.day, slot.period, currentAssignments); if (!isGapValid) continue; } // Place the lesson currentAssignments.push(lesson); placedCount++; placed = true; break; } if (!placed) { unplaced.push(lesson); } }); // Calculate conflicts for the current run const { teacherConflicts, roomConflicts, total } = calculateConflicts(currentAssignments); const conflictCount = total; // Update best result if (conflictCount < bestConflictCount) { bestConflictCount = conflictCount; bestAssignments = currentAssignments.slice(baseAssignments.length); bestUnplaced = unplaced; } // Early exit if perfect schedule if (conflictCount === 0) break; } // Apply the best schedule found if (bestAssignments.length > 0) { setAssignments(bestAssignments); showSuccess(isRTL ? "تم توليد جدول بأقل تعارضات" : "Generated schedule with minimal conflicts"); } else { showError(isRTL ? "لم يتم العثور على جدول مناسب" : "No suitable schedule found"); } setIsTransolving(false); }, 600); }; const handleApplyToSystem = () => { if (generatedAssignments.length === 0) return; let finalAssignments = []; if (rules.respectExistingLessons) { finalAssignments = [...assignments, ...generatedAssignments]; } else { const nonSelectedAssignments = assignments.filter(a => !rules.selectedClassIds.includes(a.classId)); finalAssignments = [...nonSelectedAssignments, ...generatedAssignments]; } setAssignments(finalAssignments); showSuccess(isRTL ? "تم تطبيق الجدول المولد على النظام بنجاح!" : "Generated schedule applied to system successfully!"); }; // Helper functions... const getEmployeeName = (id: string) => { const emp = employees.find(e => e.id === id); return emp ? `${emp.lastName} ${emp.firstName}` : "---"; }; const getSubjectName = (id: string) => subjects.find(s => s.id === id)?.name || "---"; const getClassName = (id: string) => classes.find(c => c.id === id)?.name || "---"; return ( <div className="space-y-8 pb-20"> <PageHeader title={isRTL ? "المولد التلقائي للجداول" : "Auto Schedule Generator"} subtitle={isRTL ? "توليد جدول دراسي ذكي وخالٍ من التعارضات بضغطة زر" : "Generate a smart, conflict-free schedule instantly"} icon={Sparkles} isRTL={isRTL} > <Button variant="outline" onClick={handleExtractFromCurrent} className="rounded-xl border-emerald-200 text-emerald-700 bg-white h-11 font-bold" > {isRTL ? "استخراج من الجدول الحالي" : "Extract from Current"} </Button> {requirements.length > 0 && ( <Button variant="ghost" onClick={handleClearRequirements} className="rounded-xl text-red-500 hover:bg-red-50 h-11 font-bold" > {isRTL ? "مسح الكل" : "Clear All"} </Button> )} </PageHeader> <div className="grid grid-cols-1 xl:grid-cols-3 gap-8"> {/* Left Column: Requirements Input & List */} <div className="xl:col-span-2 space-y-6"> <RequirementForm isRTL={isRTL} employees={employees} subjects={subjects} classes={classes} rooms={rooms} newReq={newReq} setNewReq={setNewReq} onAdd={handleAddRequirement} /> <GeneratorRulesCard isRTL={isRTL} classes={classes} rules={rules} setRules={setRules} /> <RequirementTable isRTL={isRTL} requirements={requirements} isGenerating={isGenerating} onGenerate={handleGenerate} onDelete={handleDeleteRequirement} getEmployeeName={getEmployeeName} getSubjectName={getSubjectName} getClassName={getClassName} /> </div> {/* Right Column: Generation Stats & Actions */} <div className="space-y-6"> {generationStats && ( <GenerationStatsCard isRTL={isRTL} stats={generationStats} onApply={handleApplyToSystem} /> )} {unplacedLessons.length > 0 && ( <UnplacedLessonsCard isRTL={isRTL} unplacedLessons={unplacedLessons} getEmployeeName={getEmployeeName} getSubjectName={getSubjectName} getClassName={getClassName} /> )} </div> </div> </div> ); }; export default AutoGenerator;